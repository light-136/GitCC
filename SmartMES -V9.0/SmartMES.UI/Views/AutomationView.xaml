<UserControl x:Class="SmartMES.UI.Views.AutomationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDarkBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 控制栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="▶ 启动流程" Style="{StaticResource SuccessButton}" Command="{Binding StartFlowCommand}"/>
            <Button Content="↺ 重置流程" Style="{StaticResource OrangeButton}"  Command="{Binding ResetFlowCommand}"/>
            <Border Background="{StaticResource BgLightBrush}" CornerRadius="4" Padding="12,6">
                <TextBlock Text="{Binding FlowStatus}" Foreground="{StaticResource AccentBrush}" FontWeight="Bold"/>
            </Border>
            <TextBlock Text="提示: Ctrl+F5 全局快捷键也可启动" Foreground="{StaticResource TextSecondaryBrush}"
                       VerticalAlignment="Center" FontSize="11"/>
        </StackPanel>

        <!-- 步骤卡片列表 -->
        <ItemsControl Grid.Row="2" ItemsSource="{Binding Steps}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CardPanel}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="180"/>
                            </Grid.ColumnDefinitions>

                            <!-- 状态图标 -->
                            <TextBlock Grid.Column="0" Text="{Binding StatusIcon}"
                                       FontSize="22" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding StatusIcon}" Value="✓">
                                                <Setter Property="Foreground" Value="{StaticResource AccentGreenBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding StatusIcon}" Value="✗">
                                                <Setter Property="Foreground" Value="{StaticResource AccentRedBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding StatusIcon}" Value="▶">
                                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <!-- 步骤信息 -->
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"
                                           Foreground="{StaticResource TextPrimaryBrush}" FontSize="14"/>
                                <TextBlock Text="{Binding Description}" Style="{StaticResource SecondaryText}"/>
                            </StackPanel>

                            <!-- 步骤消息 -->
                            <TextBlock Grid.Column="2" Text="{Binding Message}"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       VerticalAlignment="Center" FontSize="12"
                                       TextWrapping="Wrap"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
