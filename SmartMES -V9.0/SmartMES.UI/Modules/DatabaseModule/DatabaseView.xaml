<UserControl x:Class="SmartMES.UI.Modules.DatabaseModule.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDarkBrush}">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <!-- 左主区 -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="150"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="120"/>
            </Grid.RowDefinitions>

            <!-- 工具栏 -->
            <Border Grid.Row="0" Style="{StaticResource CardPanel}">
                <WrapPanel Orientation="Horizontal">
                    <ComboBox ItemsSource="{Binding DbTypes}" SelectedItem="{Binding SelectedDbType}" Width="110" Margin="0,0,8,0"/>
                    <Button Content="🔌 连接"    Style="{StaticResource SuccessButton}" Command="{Binding ConnectDbCommand}"    Margin="0,0,5,0"/>
                    <Button Content="🔄 加载"    Style="{StaticResource PrimaryButton}" Command="{Binding LoadRecordsCommand}"  Margin="0,0,5,0"/>
                    <Button Content="➕ 新增"    Style="{StaticResource OrangeButton}"  Command="{Binding AddRecordCommand}"    Margin="0,0,5,0"/>
                    <Button Content="🗑 删除"    Style="{StaticResource DangerButton}"  Command="{Binding DeleteRecordCommand}" Margin="0,0,5,0"/>
                    <Button Content="🎲 仿真数据" Style="{StaticResource OrangeButton}"  Command="{Binding SeedDataCommand}"     Margin="0,0,5,0"/>
                    <Button Content="📊 统计"    Style="{StaticResource PrimaryButton}" Command="{Binding QueryStatsCommand}"   Margin="0,0,14,0"/>
                    <TextBlock Text="导出:" VerticalAlignment="Center"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,5,0"/>
                    <Button Content="CSV"   Style="{StaticResource PrimaryButton}" Command="{Binding ExportCsvCommand}"   Padding="8,5" Margin="0,0,4,0"/>
                    <Button Content="JSON"  Style="{StaticResource PrimaryButton}" Command="{Binding ExportJsonCommand}"  Padding="8,5" Margin="0,0,4,0"/>
                    <Button Content="Excel" Style="{StaticResource SuccessButton}" Command="{Binding ExportExcelCommand}" Padding="8,5"/>
                </WrapPanel>
            </Border>

            <!-- 数据表格 -->
            <Border Grid.Row="2" Style="{StaticResource CardPanel}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="📋 生产记录" Style="{StaticResource HeadingText}" Margin="12,8"/>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Records}" SelectedItem="{Binding SelectedRecord}"
                              IsReadOnly="True" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID"   Binding="{Binding Id}"                         Width="45"/>
                            <DataGridTextColumn Header="工单" Binding="{Binding OrderId}"                    Width="130"/>
                            <DataGridTextColumn Header="产品" Binding="{Binding ProductCode}"                Width="60"/>
                            <DataGridTextColumn Header="数量" Binding="{Binding Qty}"                        Width="50"/>
                            <DataGridTextColumn Header="合格" Binding="{Binding IsPass}"                     Width="50"/>
                            <DataGridTextColumn Header="温度℃" Binding="{Binding Temperature,StringFormat=F1}" Width="60"/>
                            <DataGridTextColumn Header="压力" Binding="{Binding Pressure,StringFormat=F2}"   Width="55"/>
                            <DataGridTextColumn Header="时间" Binding="{Binding RecordTime,StringFormat=HH:mm:ss}" Width="70"/>
                            <DataGridTextColumn Header="操作员" Binding="{Binding Operator}"               Width="65"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- 统计结果 -->
            <Border Grid.Row="4" Style="{StaticResource CardPanel}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="📊 工单统计" Style="{StaticResource HeadingText}" Margin="12,6"/>
                    <ListBox Grid.Row="1" ItemsSource="{Binding StatsRows}"
                             Background="Transparent" BorderThickness="0"
                             FontFamily="Consolas" FontSize="11"
                             Foreground="{StaticResource AccentGreenBrush}"/>
                </Grid>
            </Border>

            <!-- 操作日志 -->
            <Border Grid.Row="6" Style="{StaticResource CardPanel}" Padding="6">
                <ListBox ItemsSource="{Binding Logs}" Background="Transparent" BorderThickness="0"
                         FontFamily="Consolas" FontSize="11"
                         Foreground="{StaticResource AccentBrush}"/>
            </Border>
        </Grid>

        <!-- 右：KV存储 -->
        <Border Grid.Column="2" Style="{StaticResource CardPanel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="🗃 KV存储" Style="{StaticResource HeadingText}"/>
                <TextBlock Grid.Row="2" Text="模拟Redis，支持TTL过期" Style="{StaticResource SecondaryText}" FontSize="10" TextWrapping="Wrap"/>
                <ListBox Grid.Row="4" ItemsSource="{Binding KvItems}" Background="Transparent"
                         BorderThickness="0" FontFamily="Consolas" FontSize="11"
                         Foreground="{StaticResource AccentBrush}"/>
                <WrapPanel Grid.Row="6" Orientation="Horizontal">
                    <Button Content="随机Set" Style="{StaticResource OrangeButton}" Command="{Binding KvSetCommand}"     Margin="0,0,4,0" Padding="8,5"/>
                    <Button Content="刷新"   Style="{StaticResource PrimaryButton}" Command="{Binding KvRefreshCommand}" Padding="8,5"/>
                </WrapPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
