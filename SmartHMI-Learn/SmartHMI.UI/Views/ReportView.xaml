<UserControl x:Class="SmartHMI.UI.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDark}">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧：导出配置 -->
        <StackPanel Grid.Column="0" Margin="0,0,12,0">
            <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="时间范围" Style="{StaticResource SectionHeader}"/>
                    <TextBlock Text="开始日期" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding FromDate}" Margin="0,0,0,8"
                                Background="{StaticResource BgCard}" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="结束日期" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding ToDate}"
                                Background="{StaticResource BgCard}" Foreground="{StaticResource TextPrimary}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="报表导出" Style="{StaticResource SectionHeader}"/>
                    <Button Content="📊  导出报警报表" Style="{StaticResource AccentButton}"
                            Command="{Binding ExportAlarmReportCommand}" Margin="0,0,0,8"/>
                    <Button Content="📈  导出生产报表" Style="{StaticResource AccentButton}"
                            Command="{Binding ExportProductionReportCommand}" Margin="0,0,0,8"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="追溯报表" Style="{StaticResource SectionHeader}"/>
                    <TextBlock Text="工单号" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding WorkorderId, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                    <Button Content="📋  导出追溯报表" Style="{StaticResource AccentButton}"
                            Command="{Binding ExportTraceReportCommand}"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- 右侧：导出历史 -->
        <Border Grid.Column="1" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="导出历史" Style="{StaticResource SectionHeader}"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource AccentAmber}"
                           FontSize="12" Margin="0,0,0,8"/>
                <ListBox ItemsSource="{Binding ExportHistory}" Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Foreground="{StaticResource AccentGreen}"
                                       FontFamily="Consolas" FontSize="12"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
