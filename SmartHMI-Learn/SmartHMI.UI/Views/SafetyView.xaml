<UserControl x:Class="SmartHMI.UI.Views.SafetyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDark}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 急停状态横幅 -->
        <Border Grid.Row="0" Margin="0,0,0,12" CornerRadius="6" Padding="16,10">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#0A4A2A"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEStopActive}" Value="True">
                            <Setter Property="Background" Value="#6E0A0A"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel Orientation="Horizontal">
                <TextBlock FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,12,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="✅  系统安全"/>
                            <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEStopActive}" Value="True">
                                    <Setter Property="Text" Value="🛑  急停已触发"/>
                                    <Setter Property="Foreground" Value="{StaticResource AccentRed}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock Text="{Binding EStopReason}" Foreground="{StaticResource AccentRed}"
                           FontSize="13" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- 操作按钮 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="🛑  触发急停" Style="{StaticResource DangerButton}"
                    Command="{Binding TriggerEStopCommand}" Margin="0,0,8,0" Width="120"/>
            <Button Content="✅  复位急停" Style="{StaticResource SuccessButton}"
                    Command="{Binding ResetEStopCommand}" Margin="0,0,8,0" Width="120"/>
            <Button Content="刷新" Style="{StaticResource BaseButton}"
                    Command="{Binding RefreshCommand}" Width="80"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondary}"
                       FontSize="12" VerticalAlignment="Center" Margin="16,0,0,0"/>
        </StackPanel>

        <!-- 安全条件列表 -->
        <Border Grid.Row="2" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="安全互锁条件" Style="{StaticResource SectionHeader}"/>
                <DataGrid ItemsSource="{Binding Conditions}" IsReadOnly="True" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="状态" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Ellipse Width="10" Height="10" HorizontalAlignment="Center">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Fill" Value="{StaticResource AccentGreen}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSafe}" Value="False">
                                                        <Setter Property="Fill" Value="{StaticResource AccentRed}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="条件名称" Binding="{Binding Name}" Width="160"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding StatusText}" Width="80"/>
                        <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
