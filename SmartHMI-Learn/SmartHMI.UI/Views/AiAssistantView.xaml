<UserControl x:Class="SmartHMI.UI.Views.AiAssistantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDark}">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="360"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧：AI 对话 -->
        <Grid Grid.Column="0" Margin="0,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- API 配置 -->
            <Border Grid.Row="0" Style="{StaticResource Card}" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="API 端点:" Foreground="{StaticResource TextSecondary}"
                               VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12"/>
                    <TextBox Grid.Column="1" Text="{Binding ApiEndpoint, UpdateSourceTrigger=PropertyChanged}"
                             FontFamily="Consolas" FontSize="11"/>
                </Grid>
            </Border>

            <!-- 对话历史 -->
            <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,12">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding ChatHistory}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,4" Padding="10,8" CornerRadius="6">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{StaticResource BgPanel}"/>
                                            <Setter Property="HorizontalAlignment" Value="Left"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                    <Setter Property="Background" Value="#0A3A5A"/>
                                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel MaxWidth="500">
                                        <TextBlock Text="{Binding Role}" FontSize="10" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource AccentCyan}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource AccentAmber}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="{Binding Content}" Foreground="{StaticResource TextPrimary}"
                                                   FontSize="13" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                        <TextBlock Text="{Binding Timestamp, StringFormat={}{0:HH:mm:ss}}"
                                                   Foreground="{StaticResource TextSecondary}" FontSize="10" HorizontalAlignment="Right"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <!-- 输入框 -->
            <Border Grid.Row="2" Style="{StaticResource Card}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding ChatInput, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,8,0" FontSize="13"/>
                    <Button Grid.Column="1" Content="发送" Style="{StaticResource AccentButton}"
                            Command="{Binding SendChatCommand}" Width="80" Margin="0,0,8,0"/>
                    <Button Grid.Column="2" Content="清空" Style="{StaticResource BaseButton}"
                            Command="{Binding ClearChatCommand}" Width="60"/>
                </Grid>
            </Border>
        </Grid>

        <!-- 右侧：AI 分析建议 -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource Card}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="AI 分析" Style="{StaticResource SectionHeader}"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,8"/>
                    <Button Content="🔔  分析报警" Style="{StaticResource AccentButton}" Command="{Binding AnalyzeAlarmsCommand}" Margin="0,0,0,6"/>
                    <Button Content="🖥  分析设备健康" Style="{StaticResource BaseButton}" Command="{Binding AnalyzeDeviceCommand}" Margin="0,0,0,6"/>
                    <Button Content="📈  分析生产趋势" Style="{StaticResource BaseButton}" Command="{Binding AnalyzeProductionCommand}"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="AI 建议列表" Style="{StaticResource SectionHeader}"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="500">
                        <ItemsControl ItemsSource="{Binding Suggestions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,4" Padding="10,8" CornerRadius="4" Background="{StaticResource BgPanel}">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="12">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{StaticResource AccentCyan}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Priority}" Value="Critical">
                                                                <Setter Property="Foreground" Value="{StaticResource AccentRed}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Priority}" Value="High">
                                                                <Setter Property="Foreground" Value="{StaticResource AccentAmber}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <TextBlock Text="{Binding Content}" Foreground="{StaticResource TextPrimary}"
                                                       FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                            <TextBlock Foreground="{StaticResource TextSecondary}" FontSize="10" Margin="0,4,0,0">
                                                <Run Text="{Binding Source}"/>
                                                <Run Text=" | 置信度: "/>
                                                <Run Text="{Binding Confidence, StringFormat={}{0:P0}}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
