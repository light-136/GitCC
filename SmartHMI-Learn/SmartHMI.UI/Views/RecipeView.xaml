<UserControl x:Class="SmartHMI.UI.Views.RecipeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDark}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="激活配方" Style="{StaticResource AccentButton}"
                    Command="{Binding SetActiveCommand}" Margin="0,0,8,0"/>
            <Button Content="删除" Style="{StaticResource DangerButton}"
                    Command="{Binding DeleteCommand}" Margin="0,0,8,0"/>
            <Button Content="刷新" Style="{StaticResource BaseButton}"
                    Command="{Binding RefreshCommand}" Margin="0,0,16,0"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource AccentGreen}"
                       FontSize="12" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 配方列表 -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,12">
            <DataGrid ItemsSource="{Binding Recipes}" SelectedItem="{Binding SelectedRecipe}"
                      IsReadOnly="True" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="激活" Width="50">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="●" HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="配方名称" Binding="{Binding Name}" Width="180"/>
                    <DataGridTextColumn Header="版本" Binding="{Binding Version}" Width="60"/>
                    <DataGridTextColumn Header="产品类型" Binding="{Binding ProductType}" Width="120"/>
                    <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="创建人" Binding="{Binding CreatedBy}" Width="80"/>
                    <DataGridTextColumn Header="修改时间" Binding="{Binding ModifiedAt, StringFormat={}{0:MM-dd HH:mm}}" Width="110"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- 新增配方 -->
        <Border Grid.Row="2" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="新增配方" Style="{StaticResource SectionHeader}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,8,0" Tag="配方名称"/>
                    <TextBox Grid.Column="1" Text="{Binding NewProductType, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,8,0" Tag="产品类型"/>
                    <Button Grid.Column="2" Content="添加" Style="{StaticResource AccentButton}"
                            Command="{Binding AddCommand}" Width="80"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
