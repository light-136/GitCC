# DataScope Studio 详细设计文档

- 文档编号：DS-DESIGN-03
- 关联文档：《02-总体设计文档.md》
- 说明：本文档基于工程真实源码逐类编写；所有类名、方法签名、信号槽均与 `src/` 下的实现一致。

---

## 1. 各模块类清单与职责表

### 1.1 utils 工具层（datascope_utils）

| 类 / 命名空间 | 文件 | 职责 |
| --- | --- | --- |
| `datascope::utils::ByteUtils` | `src/utils/byteutils.h/cpp` | 静态工具类：字节数组 ↔ hex 字符串互转；大端序读写 16/32 位整数 |
| `datascope::utils::TimeUtils` | `src/utils/timeutils.h/cpp` | 静态工具类：时间格式化（数据打点）、解析、间隔换算、文件友好时间戳 |

### 1.2 infrastructure 基础设施层（datascope_infra）

| 类 | 文件 | 职责 |
| --- | --- | --- |
| `datascope::infrastructure::LogLevel` | `src/infrastructure/logmanager.h` | 日志级别强类型枚举（Trace..Fatal） |
| `datascope::infrastructure::LogManager` | `src/infrastructure/logmanager.h/cpp` | 全局单例日志：初始化日志目录、线程安全写盘、按天滚动、`messageLogged` 信号广播 |
| `datascope::infrastructure::ConfigManager` | `src/infrastructure/configmanager.h/cpp` | 全局单例 INI 键值配置：类型安全读写、线程安全、懒加载 |

### 1.3 domain 领域模型层（datascope_domain）

| 类型 | 文件 | 职责 |
| --- | --- | --- |
| `datascope::domain::DeviceStatus` | `src/domain/models.h` | 强类型枚举：Disconnected / Connecting / Connected / Error |
| `datascope::domain::Channel` | `src/domain/models.h` | 值类型 struct：通道描述（index/name/unit/scale/offset） |
| `datascope::domain::DataPoint` | `src/domain/models.h` | 值类型 struct：一次采样点（channelIndex/value/timestamp） |
| `datascope::domain::Device` | `src/domain/models.h/cpp` | class：设备（id/name/status + 通道集合的封装管理） |
| `datascope::domain::v2::DataPoint` | `src/domain/domainmodel_v2.h` | V2 采样点：channelIndex/rawValue/engValue/quality/timestamp（报警引擎输入） |
| `datascope::domain::v2::DataQuality` | `src/domain/domainmodel_v2.h` | 数据质量枚举（Good/…，告警与判脏用） |
| `datascope::domain::v2::AlarmCondition` | `src/domain/domainmodel_v2.h` | 报警条件枚举（如 AboveHigh） |
| `datascope::domain::v2::AlarmRule` | `src/domain/domainmodel_v2.h` | 报警规则：channelIndex + condition + threshold + description |
| `datascope::domain::v2::AlarmSeverity` | `src/domain/domainmodel_v2.h` | 报警级别枚举（Info/Warning/Critical…） |
| `datascope::domain::v2::AlarmEvent` | `src/domain/domainmodel_v2.h` | 报警事件（id/rule/channel/severity/text/timestamp，报警中心列表数据） |

### 1.4 protocol 协议引擎层（datascope_protocol）

| 类 | 文件 | 职责 |
| --- | --- | --- |
| `datascope::protocol::ProtocolFrame` | `src/protocol/protocoltypes.h` | 解析结果结构体：func/cmd/payload/isValid |
| 帧常量 | `src/protocol/protocoltypes.h` | `kSof`/`kMaxPayloadLen`/`kHeaderLen`/`kCrcLen` |
| `datascope::protocol::Crc16` | `src/protocol/crc16.h/cpp` | MODBUS CRC16 静态工具（反射表驱动） |
| `datascope::protocol::FrameBuilder` | `src/protocol/framebuilder.h/cpp` | 静态组帧器：字段 → 完整帧字节流 |
| `datascope::protocol::FrameParser` | `src/protocol/frameparser.h/cpp` | 流式解析器：三态状态机，处理半包/粘包/错位重同步 |
| `datascope::protocol::ChannelConfigInfo` | `src/protocol/channelconfigcodec.h` | 值类型 struct：通道配置（index/name/unit/rangeMin/rangeMax），跨层传递通道元数据 |
| `datascope::protocol::ChannelConfigCodec` | `src/protocol/channelconfigcodec.h/cpp` | 通道配置编解码器（V2-执行③）：QVector<ChannelConfigInfo> ↔ 协议载荷字节（33B/通道，防御性） |

### 1.5 services 服务层（datascope_services）

| 类 | 文件 | 职责 |
| --- | --- | --- |
| `datascope::services::TcpClient` | `src/services/tcpclient.h/cpp` | TCP 客户端封装：连接/断开/发帧/收帧，内部挂 `QTcpSocket` + `FrameParser` |
| `datascope::services::AcquisitionWorker` | `src/services/acquisitionworker.h/cpp` | 采集线程工作对象：驱动 TcpClient，把帧转换为 DataPoint 并投递回主线程 |
| `datascope::services::DataService` | `src/services/dataservice.h/cpp` | 数据总线：连接/采集控制 + 数据分发 + 最新快照 |
| `datascope::services::DeviceController` | `src/services/devicecontroller.h/cpp` | 设备状态机控制器：状态转移 + 退避自动重连 |
| `datascope::services::RecordManager` | `src/services/recordmanager.h/cpp` | 记录（CSV 落盘）与回放（定时逐帧重放） |

### 1.6 UI 层（主程序 DataScope）

| 类 | 文件 | 职责 |
| --- | --- | --- |
| `MainWindow` | `src/ui/mainwindow.h/cpp` | 主窗口：菜单/工具栏/中央 5 页签/状态栏/QSS 深色主题；组装 DataService、RecordManager 并接线 |
| `datascope::ui::MonitorPage` | `src/ui/pages/monitorpage.h/cpp` | 监控总览页：通道卡片（LED+数值+曲线+仪表）**由设备上报配置数据驱动**（名称/单位/量程/数量），唯一数据入口 `onDataUpdated` |
| `datascope::ui::DevicePage` | `src/ui/pages/devicepage.h/cpp` | 设备管理页（V2）：设备配置列表 + ConfigManager 持久化 + 连接/断开控制 |
| `datascope::ui::RecordPage` | `src/ui/pages/recordpage.h/cpp` | 记录/回放页：按钮编排 + 进度条 |
| `datascope::ui::SettingsPage` | `src/ui/pages/settingspage.h/cpp` | 设置页（P7 接入 ConfigManager UI）：主题切换等 |
| `datascope::ui::LogPanel` | `src/ui/widgets/logpanel.h/cpp` | 日志面板（V2-执行③）：订阅 LogManager::messageLogged，过滤/着色/上限缓冲，主窗口第 5 页签 |
| `datascope::app::SignalSlotDemo` | `src/app/signalslotdemo.h/cpp` | 信号槽五种连接方式教学演示对象（V2 起随学习模式提供，不占主界面页签） |
| `datascope::ui::LineChartWidget` | `src/ui/widgets/linechartwidget.h/cpp` | 自绘实时滚动曲线 |
| `datascope::ui::GaugeWidget` | `src/ui/widgets/gaugewidget.h/cpp` | 自绘仪表盘 |
| `datascope::ui::LedIndicator` | `src/ui/widgets/ledindicator.h/cpp` | 自绘 LED 指示灯 |

### 1.7 模拟设备层（datascope_simulator）

| 类 / 枚举 | 文件 | 职责 |
| --- | --- | --- |
| `datascope::simulator::SimulatorMode` | `src/simulator/simulatordevice.h` | 运行模式枚举：Normal / Alarm / Stress / Busy / Error |
| `datascope::simulator::FaultType` | `src/simulator/faultinjector.h` | 异常类型枚举：None/Sticky/Fragment/CrcError/IllegalFrame/Delay/Surge/Disconnect |
| `datascope::simulator::FaultConfig` | `src/simulator/faultinjector.h` | 异常注入配置 struct：type/period/channelIndex/surgeValue |
| `datascope::simulator::FaultInjector` | `src/simulator/faultinjector.h/cpp` | 纯逻辑异常注入器：正常帧 → 0..N 段异常字节流（无 TCP 依赖，可单测可复用） |
| `datascope::simulator::SimulatorDevice` | `src/simulator/simulatordevice.h/cpp` | 模拟采集设备：QTcpServer 服务端 + 周期数据帧 + 模式/异常注入 + 通道配置应答 |

---

## 2. 核心类详细设计

### 2.1 `datascope::services::TcpClient`

- **继承**：`public QObject`
- **定位**：服务层；封装 `QTcpSocket`，对外提供"连接/断开/发帧/收帧"四个业务动作。

**关键成员：**

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| `m_socket` | `QTcpSocket*` | 底层 socket，构造时 `new QTcpSocket(this)`，挂 QObject 对象树回收 |
| `m_parser` | `protocol::FrameParser` | 流式帧解析器（值成员），处理半包/粘包 |

**方法签名与职责：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| 构造 | `explicit TcpClient(QObject *parent = nullptr)` | 创建 socket；连接 `connected`/`disconnected`/`error`(QOverload)/`readyRead` 四个原生信号到内部槽 |
| 连接 | `void connectToHost(const QString &host, quint16 port)` | 异步发起连接（`m_socket->connectToHost`），结果由信号通知 |
| 断开 | `void disconnectFromHost()` | 主动断开 |
| 查询 | `bool isConnected() const` | 仅 `ConnectedState` 算已连接 |
| 发帧 | `bool sendFrame(const protocol::ProtocolFrame &frame)` | 未连接直接 false；`FrameBuilder::build` 组帧（空数组=false）；`m_socket->write` 全量写出 |

**信号：**

| 信号 | 签名 | 触发时机 |
| --- | --- | --- |
| `connected` | `void connected()` | socket 连接成功 |
| `disconnected` | `void disconnected()` | 主动断开/对端关闭/网络异常 |
| `errorOccurred` | `void errorOccurred(const QString &message)` | 底层 socket 错误统一转 QString |
| `frameReceived` | `void frameReceived(const protocol::ProtocolFrame &frame)` | 每解析出一帧合法帧（CRC 通过） |

**关键实现（`onReadyRead`）：**

```cpp
const QByteArray chunk = m_socket->readAll();   // 一次读走全部就绪字节
m_parser.feed(chunk);                           // 喂解析器（只追加）
protocol::ProtocolFrame frame;
while (m_parser.nextFrame(frame))               // 循环取帧：粘包场景可出多帧
    emit frameReceived(frame);
```

### 2.2 `datascope::services::AcquisitionWorker`

- **继承**：`public QObject`
- **定位**：采集线程工作对象，被 `moveToThread()` 迁到采集线程；`TcpClient` 在 `start()` 槽内创建，确保 socket 与线程同生。

**关键成员：**

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| `m_client` | `TcpClient*` | 采集线程内的 TCP 客户端，`start()` 时创建、`stop()`/错误时销毁 |

**公开槽（均在采集线程内执行）：**

| 槽 | 签名 | 职责 |
| --- | --- | --- |
| `start` | `void start(const QString &host, quint16 port)` | 防重入（已存在 m_client 直接 return）；new TcpClient(this)；接 `connected`/`disconnected`/`errorOccurred`/`frameReceived` 四个信号；发起 `connectToHost`。错误时 `deleteLater()` 并置空 m_client——这是自动重连能工作的关键 |
| `stop` | `void stop()` | `m_client->disconnectFromHost()` → `deleteLater()` → 置空 |

**信号：**

| 信号 | 签名 | 说明 |
| --- | --- | --- |
| `pointsReady` | `void pointsReady(const QVector<datascope::domain::DataPoint> &points)` | 解析出的一批采样点（跨线程投递主线程） |
| `connectedChanged` | `void connectedChanged(bool connected)` | 连接状态变化 |
| `errorOccurred` | `void errorOccurred(const QString &message)` | 链路错误统一字符串 |
| `channelConfigReceived` | `void channelConfigReceived(const QVector<datascope::protocol::ChannelConfigInfo> &channels)` | 设备应答的通道配置（V2-执行③，解码后跨线程投递） |

**V2-执行③ 关键行为：**

1. **连接成功后主动查配置**：`start()` 的 connected lambda 中，`emit connectedChanged(true)` 之后发送一帧查询请求 `{func=kFuncQuery(0x03), cmd=kCmdQueryChannels(0x01)}`——监控页所需通道元数据由设备主动上报，不再主机硬编码。
2. **handleFrame 分流**：收到帧先按功能码/子命令分流，配置响应帧不进采集数据通道：

```cpp
void handleFrame(const protocol::ProtocolFrame &frame) {
    // 分流 1：通道配置响应（0x83/0x01）→ 解码上报，不进数据通道
    if (frame.func == protocol::kFuncQueryResponse
        && frame.cmd == protocol::kCmdQueryChannels) {
        QVector<protocol::ChannelConfigInfo> channels;
        if (protocol::ChannelConfigCodec::decode(frame.payload, channels))
            emit channelConfigReceived(channels);
        else
            emit errorOccurred(QStringLiteral("通道配置帧解码失败（载荷结构非法）"));
        return;
    }
    // 分流 2：采集数据帧（0x01/0x01）→ 大端 float 解码为 DataPoint
    constexpr int kFloatBytes = 4;
    const int channelCount = frame.payload.size() / kFloatBytes;
    if (channelCount <= 0) return;
    QVector<domain::DataPoint> points;
    for (int i = 0; i < channelCount; ++i) {
        // 大端 4 字节 → quint32 → memcpy 位模式解释为 float
        float value = 0.0f; memcpy(&value, &raw, sizeof(value));
        domain::DataPoint dp;
        dp.channelIndex = i; dp.value = value; dp.timestamp = QDateTime::currentDateTime();
        points.append(dp);
    }
    emit pointsReady(points);
}
```

### 2.3 `datascope::services::DataService`

- **继承**：`public QObject`
- **定位**：主线程与采集线程之间的**唯一数据通道**（数据总线）。UI 只认它。

**关键成员：**

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| `m_thread` | `QThread*` | 采集线程（objectName="AcquisitionThread"） |
| `m_worker` | `AcquisitionWorker*` | 采集线程工作对象（moveToThread） |
| `m_mutex` | `QMutex*` | 保护快照与状态标志 |
| `m_lastData` | `QVector<domain::DataPoint>` | 最新数据快照 |
| `m_connected` / `m_acquiring` | `bool` | 连接/采集状态标志 |

**方法签名与职责：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| 构造 | `explicit DataService(QObject *parent = nullptr)` | 注册 `QVector<DataPoint>` 元类型；创建线程+Worker+moveToThread；连接 Worker 信号；`m_thread->start()` |
| 析构 | `~DataService() override` | `stopAcquisition()` → `quit()` → `wait(3000)`；释放互斥锁 |
| 连接 | `void connectTo(const QString &host, quint16 port)` | 已连接直接报错；`invokeMethod(worker,"start",QueuedConnection)` |
| 断开 | `void disconnectFromDevice()` | 简化：等价 `stopAcquisition()` |
| 查询 | `bool isConnected() const` / `bool isAcquiring() const` | 锁内读状态 |
| 采集 | `void startAcquisition()` / `void stopAcquisition()` | 启/停采集；`stopAcquisition` 同时 `invokeMethod` 投递 `Worker::stop` |
| 快照 | `QVector<domain::DataPoint> lastData() const` | 锁内返回快照拷贝 |

**信号：**

| 信号 | 签名 | 说明 |
| --- | --- | --- |
| `connected` | `void connected()` | 连接成功（主线程收到） |
| `disconnected` | `void disconnected()` | 断开 |
| `connectionError` | `void connectionError(const QString &message)` | 连接/采集错误 |
| `dataUpdated` | `void dataUpdated(const QVector<datascope::domain::DataPoint> &points)` | 实时数据分发（MonitorPage 消费） |
| `acquisitionStarted` / `acquisitionStopped` | `void` | 采集启停通知 |
| `channelConfigReceived` | `void channelConfigReceived(const QVector<datascope::protocol::ChannelConfigInfo> &channels)` | 通道配置上报（V2-执行③）：Worker 解码 0x83 配置帧 → 主线程转发 |

**V2-执行③ 元类型注册**：跨线程投递 `QVector<ChannelConfigInfo>` 需要 `Q_DECLARE_METATYPE` + `qRegisterMetaType`，且注册名与 moc 规范化名一致（`"QVector<datascope::protocol::ChannelConfigInfo>"`、`"QVector<ChannelConfigInfo>"` 等多个名称兜底）。

**关键实现（`onPointsReady` / `onConnectedChanged`，主线程槽）：**

```cpp
void DataService::onPointsReady(const QVector<domain::DataPoint> &points) {
    { QMutexLocker locker(m_mutex); m_lastData = points; }   // 覆盖式快照
    emit dataUpdated(points);
}
void DataService::onConnectedChanged(bool nowConnected) {
    { QMutexLocker locker(m_mutex); m_connected = nowConnected; }
    if (nowConnected) emit connected(); else emit disconnected();
}
```

### 2.4 `datascope::services::DeviceController`

- **继承**：`public QObject`
- **定位**：设备状态机控制器，把 DataService 的"无状态"信号翻译成"有状态"状态机，并实现退避自动重连。

**关键成员：**

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| `m_service` | `DataService*` | 数据总线（构造时 `new DataService(this)`） |
| `m_reconnectTimer` | `QTimer*` | 重连定时器（单次触发） |
| `m_status` | `DeviceStatus` | 当前状态（初始 Disconnected） |
| `m_userDisconnected` | `bool` | 用户主动断开标志 |
| `m_retryAttempts` | `int` | 重连尝试计数（驱动退避间隔） |
| `m_host` / `m_port` | `QString` / `quint16` | 重连目标 |

**方法签名与职责：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| 构造 | `explicit DeviceController(QObject *parent = nullptr)` | 创建 DataService + 单次 QTimer；连接 DataService 四个信号与定时器 timeout |
| 查询 | `datascope::domain::DeviceStatus status() const` | 当前状态 |
| 连接 | `void connectDevice(const QString &host, quint16 port)` | Connected 时忽略；保存 host/port；`m_userDisconnected=false`、`m_retryAttempts=0`；停定时器；`setStatus(Connecting)`；`m_service->connectTo` |
| 断开 | `void disconnectDevice()` | `m_userDisconnected=true`；停定时器；`m_service->disconnectFromDevice()`；`setStatus(Disconnected)` |
| 采集 | `void startAcquisition()` / `void stopAcquisition()` | 透传给 DataService |

**信号：**

| 信号 | 签名 | 说明 |
| --- | --- | --- |
| `statusChanged` | `void statusChanged(datascope::domain::DeviceStatus status)` | 状态转移通知（状态不变不重发） |
| `errorOccurred` | `void errorOccurred(const QString &message)` | 错误/降级通知（连接失败、断线、重连提示） |
| `dataUpdated` | `void dataUpdated(const QVector<datascope::domain::DataPoint> &points)` | 实时数据转发 |

**私有槽：**

| 槽 | 签名 | 职责 |
| --- | --- | --- |
| `onServiceConnected` | `void` | `m_userDisconnected=false`、`m_retryAttempts=0`；停定时器；`setStatus(Connected)` |
| `onServiceDisconnected` | `void` | 用户主动断开→`setStatus(Disconnected)`；否则→`setStatus(Error)` + `startReconnectTimer()` |
| `onServiceError` | `void onServiceError(const QString &message)` | emit errorOccurred；`setStatus(Error)`；非用户主动断开则 `startReconnectTimer()` |
| `onDataUpdated` | `void onDataUpdated(const QVector<domain::DataPoint> &points)` | 透传 `emit dataUpdated(points)` |
| `onReconnectTimeout` | `void` | `setStatus(Connecting)`；`m_service->connectTo(m_host, m_port)` |

**私有工具：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| `setStatus` | `void setStatus(DeviceStatus s)` | 状态不变直接 return；否则更新并 `emit statusChanged(s)` |
| `startReconnectTimer` | `void` | 仅 Error 态且非用户主动断开且未激活时生效；间隔 `min(1000 << m_retryAttempts, 30000)`；`++m_retryAttempts`；emit "设备连接失败，X 秒后自动重连..."；`m_reconnectTimer->start(intervalMs)` |
| `stopReconnectTimer` | `void` | 停定时器 |

### 2.5 `datascope::services::RecordManager`

- **继承**：`public QObject`
- **定位**：记录（CSV 落盘）与回放（定时逐帧重放）。

**关键成员：**

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| `m_recordFile` | `QFile` | CSV 输出文件（打开=正在记录） |
| `m_recording` | `bool` | 记录开关 |
| `m_replayFrames` | `QVector<QVector<domain::DataPoint>>` | 已加载回放帧（每帧=同一时间戳的全部通道） |
| `m_replayIndex` | `int` | 下一帧要发的下标 |
| `m_replayTimer` | `QTimer*` | 回放节拍定时器 |

**方法签名与职责：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| 构造 | `explicit RecordManager(QObject *parent = nullptr)` | 创建 QTimer（默认 100ms），连接 timeout → `onReplayTick` |
| 开始记录 | `bool startRecording(const QString &filePath)` | 先 `stopRecording()`；打开 `WriteOnly|Text|Truncate`；写表头；`m_recording=true` |
| 停止记录 | `void stopRecording()` | flush + close；`m_recording=false` |
| 查询 | `bool isRecording() const` | 记录开关 |
| 追加 | `void appendData(const QVector<domain::DataPoint> &points)` | 未记录时静默忽略；逐点写 `timestampMs,channelIndex,value` 行 |
| 加载回放 | `bool loadReplay(const QString &filePath)` | 读 CSV，跳过表头；按时间戳哈希分组为帧；按时间戳升序排序存入 `m_replayFrames`；`m_replayIndex=0` |
| 开始回放 | `void startReplay(int intervalMs = 100)` | 空帧报错；`stopReplay()` 复位；设置间隔并启动定时器 |
| 停止回放 | `void stopReplay()` | 停定时器 |
| 查询 | `bool isReplaying() const` / `int replayFrameCount() const` / `int replayProgress() const` | 回放状态与进度 |

**信号：**

| 信号 | 签名 | 说明 |
| --- | --- | --- |
| `replayData` | `void replayData(const QVector<datascope::domain::DataPoint> &points)` | 回放发出一帧 |
| `replayFinished` | `void replayFinished()` | 回放结束 |
| `progressChanged` | `void progressChanged(int current)` | 进度（已发帧数） |
| `errorOccurred` | `void errorOccurred(const QString &message)` | 文件无法打开/解析失败 |

**关键实现（`onReplayTick`）：**

```cpp
void RecordManager::onReplayTick() {
    if (m_replayIndex >= m_replayFrames.size()) {
        stopReplay(); emit replayFinished(); return;
    }
    emit replayData(m_replayFrames.at(m_replayIndex));
    ++m_replayIndex;
    emit progressChanged(m_replayIndex);
}
```

### 2.6 `datascope::protocol::Crc16`

- **继承**：无（静态工具类）
- **定位**：MODBUS CRC16 计算。

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| `compute` | `static quint16 compute(const QByteArray &data)` | 初始 `0xFFFF`，查表驱动：`crc = (crc >> 8) ^ table.values[(crc ^ byte) & 0xFF]` |

- 私有实现：匿名命名空间内 `Crc16Table` 结构体（构造函数一次性算好 256 项表），`crc16Table()` 返回函数局部 static 表（线程安全，仅构造一次）。

### 2.7 `datascope::protocol::FrameBuilder`

- **继承**：无（静态工具类）
- **定位**：组帧器，与 FrameParser 严格互逆。

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| `build` | `static QByteArray build(quint8 func, quint8 cmd, const QByteArray &payload)` | 载荷超 `kMaxPayloadLen` 返回空数组；否则按 SOF→FUNC→CMD→LEN(大端)→DATA→CRC16(低字节在前) 组帧 |

### 2.8 `datascope::protocol::FrameParser`

- **继承**：无（值类型解析器）
- **定位**：流式解析器，处理半包/粘包/错位重同步。

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| `feed` | `void feed(const QByteArray &chunk)` | 追加字节到内部缓冲（不做解析） |
| `nextFrame` | `bool nextFrame(ProtocolFrame &out)` | 三态状态机尝试取帧；成功写 out（isValid=true）并消费；失败不破坏缓冲 |
| `bufferedBytes` | `int bufferedBytes() const` | 未消费字节数 |

**关键成员：** `QByteArray m_buffer`（累积缓冲）。
**私有辅助：** 匿名命名空间内 `findSof()` 逐字节重叠扫描 `0xAA 0x55`（`"0xAA 0x00 0xAA 0x55"` 场景下第二个才是帧头，必须逐字节推进）。

### 2.9 `datascope::ui::LineChartWidget`

- **继承**：`public QWidget`
- **定位**：自绘实时滚动曲线。

**关键成员：** `QString m_title`；`double m_min/m_max`（量程）；`QVector<double> m_data`（数据缓冲，头旧尾新）；`int m_maxPoints = 500`（缓冲上限）。

**方法签名与职责：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| 构造 | `explicit LineChartWidget(QWidget *parent = nullptr)` | 背景透明 |
| 追加 | `void appendPoint(double value)` | 尾部追加；超上限 `removeFirst()` 淘汰最旧点；`update()` |
| 量程 | `void setRange(double min, double max)` | `min>=max` 忽略；否则更新并重绘 |
| 标题 | `void setTitle(const QString &title)` | 更新标题 |
| 清空 | `void clearData()` | 清缓冲 |
| 查询 | `int pointCount() const` / `double lastValue() const` / `int maxPoints() const` / `double minimum() const` / `double maximum() const` | 只读访问器 |
| 绘制 | `void paintEvent(QPaintEvent *event) override` | 网格线+刻度（4 段/5 条）、折线（QPainterPath）、空数据提示、单点画圆点、标题/最新值 |
| 尺寸 | `QSize sizeHint() const override` / `QSize minimumSizeHint() const override` | 400x200 / 200x100 |

**坐标映射：** X 固定步进 = 绘图区宽 / (缓冲上限-1)，最新点锚定右端；Y 线性映射，越界钳制到绘图区上下沿。

### 2.10 `datascope::ui::GaugeWidget`

- **继承**：`public QWidget`
- **定位**：自绘仪表盘（扇形刻度 + 指针 + 数值/单位）。

**关键成员：** `double m_min/m_max/m_value`；`QString m_unit`。`m_value` 恒在量程内（钳制）；`setValue` 遇 NaN 落到量程下限。

**方法签名与职责：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| 构造 | `explicit GaugeWidget(QWidget *parent = nullptr)` | 背景透明 |
| 量程 | `void setRange(double min, double max)` | `min>=max` 忽略；量程变化后重新钳制当前值 |
| 数值 | `void setValue(double value)` | NaN→量程下限；越界钳制；`update()` |
| 单位 | `void setUnit(const QString &unit)` | 设置单位文本 |
| 查询 | `double value() const` / `double minimum() const` / `double maximum() const` | 只读访问器 |
| 绘制 | `void paintEvent(QPaintEvent *event) override` | 底色弧（270°）、前景弧（按比例）、刻度线（每 10%/5%）、刻度标签（0/25/50/75/100%）、指针三角形、表轴、数值/单位 |
| 尺寸 | `QSize sizeHint() const override` / `QSize minimumSizeHint() const override` | 200x200 / 120x120 |

**角度约定：** Qt 角度单位 1/16 度；表盘从 225° 顺时针扫 270° 到 -45°；指针角度 `= 225 - 比例 × 270`，用三角函数换算为方向向量。

### 2.11 `datascope::ui::LedIndicator`

- **继承**：`public QWidget`
- **定位**：自绘 LED 指示灯（亮=发光色，灭=暗灰）。

**关键成员：** `bool m_on = false`；`QColor m_color = QColor(0x16,0xa0,0x85)`。

**方法签名与职责：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| 构造 | `explicit LedIndicator(QWidget *parent = nullptr)` | 背景透明 |
| 状态 | `void setState(bool on)` | 状态未变短路；否则更新并重绘 |
| 灯色 | `void setColor(const QColor &color)` | 相等短路；更新灯色 |
| 查询 | `bool isOn() const` | 当前亮灭 |
| 绘制 | `void paintEvent(QPaintEvent *event) override` | 灯座（外圈暗化/深灰）、灯芯（QRadialGradient 渐变）、亮态高光点 |
| 尺寸 | `QSize sizeHint() const override` / `QSize minimumSizeHint() const override` | 20x20 / 12x12 |

### 2.12 `MainWindow`

- **继承**：`public QMainWindow`
- **定位**：主窗口骨架 + 全局组装与信号布线。

**关键成员：**

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| `m_tabs` | `QTabWidget*` | 中央页签 |
| `m_monitorPage`/`m_devicePage`/`m_recordPage`/`m_settingsPage` | 各页面指针 | 4 个业务页面 |
| `m_logPanel` | `datascope::ui::LogPanel*` | 日志面板（V2-执行③，第 5 页签） |
| `m_service` | `datascope::services::DataService*` | 数据总线 |
| `m_recordManager` | `datascope::services::RecordManager*` | 记录/回放管理器 |
| `m_actConnect`/`m_actDisconnect`/`m_actStart`/`m_actStop` | `QAction*` | 工具栏动作（含 V2 断开入口） |
| `m_connLabel`/`m_timeLabel` | `QLabel*` | 状态栏连接指示/时钟 |
| `m_clock` | `QTimer*` | 每秒刷新时钟 |

**私有方法：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| `buildMenuBar` | `void` | 文件（退出 Ctrl+Q）/帮助（关于）菜单 |
| `buildToolBar` | `void` | 连接设备/断开设备/启动采集/停止采集四个动作 |
| `buildCentralTabs` | `void` | 5 个页签（监控/设备/记录/设置/日志）组装到 QTabWidget；`m_monitorPage->setDemoRunning(false)` 停用演示源 |
| `buildStatusBar` | `void` | 消息区 + 连接标签 + 时钟标签 |
| `applyStyleSheet` | `void` | 加载 `:/qss/main.qss` 深色主题 |
| `connectDevice` | `void` | 从 `ConfigManager` 读 `device/defaultHost`(默认 127.0.0.1) / `device/defaultPort`(默认 40001) → `m_service->connectTo(host, port)` + `startAcquisition()`（V2 去硬编码） |
| `disconnectDevice` | `void` | 停止采集 + 断开 + 按钮/连接标签复位（V2 明确断开入口） |
| `stopAcquisition` | `void` | `m_service->stopAcquisition()`；复位按钮 |
| `onServiceConnected` | `void` | 按钮/连接标签置"已连接" |
| `onServiceDisconnected` | `void` | 按钮/连接标签复位 |
| `onServiceError` | `void onServiceError(const QString &message)` | 状态栏红字提示 + 按钮复位 |
| `onThemeChanged` | `void onThemeChanged(const QString &theme)` | 设置页主题切换：深色加载 QSS / 亮色清空 |

**构造中的关键布线（数据源可替换 + 通道配置驱动）：**

```cpp
connect(m_service, &DataService::dataUpdated, m_monitorPage, &MonitorPage::onDataUpdated);
connect(m_service, &DataService::dataUpdated, m_recordManager, &RecordManager::appendData);
connect(m_recordManager, &RecordManager::replayData, m_monitorPage, &MonitorPage::onDataUpdated);
// V2-执行③：设备上报通道配置 → 监控页数据驱动渲染
connect(m_service, &DataService::channelConfigReceived,
        m_monitorPage, &MonitorPage::onChannelConfigReceived);
```

**V2-执行③ 关键路径日志**：`connectDevice`/`disconnectDevice`/`stopAcquisition`/`onServiceConnected`/`onServiceDisconnected`/`onServiceError` 均调用 `LogManager::instance().info/warn/error(...)` 写关键动作——实时流入日志面板，形成可追溯的运行轨迹。

### 2.13 `datascope::ui::MonitorPage`

- **继承**：`public QWidget`
- **定位**：监控总览页。V2-执行③ 起**由设备上报配置数据驱动**：通道名/单位/量程/数量全部来自 `m_channelConfigs`，设备应答前用默认 4 通道兜底。

**关键成员：**

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| `m_channelConfigs` | `QVector<datascope::protocol::ChannelConfigInfo>` | 通道配置（设备上报后整体替换；驱动名称/单位/量程/数量） |
| `m_channelArea` | `QVBoxLayout*` | 通道卡片容器（可整体重建） |
| `m_channels` | `QVector<ChannelUi>` | 每通道控件组（chart/gauge/led/nameLabel/valueLabel） |
| `m_alarmEngine` / `m_alarmModel` | `AlarmEngine*` / `AlarmEventModel*` | 报警引擎 / 报警事件列表模型（V2，与 View 分离可单测） |
| `m_alarmList` | `QListView*` | 报警中心列表（`setModel(m_alarmModel)`） |
| `m_recoverId` | `int` | 报警恢复事件自增 id |
| `kAlarmRatio` | `static constexpr double = 0.85` | 报警阈值比例：值 > `rangeMax × 0.85` 触发报警 |
| `m_demoTimer` / `m_phase` / `m_demoRunning` | `QTimer*` / `double` / `bool` | 演示数据源（仅教学用，P12 后停用） |
| `kChannelCount` | `static constexpr int = 4` | 仅演示源数组长度（真实通道数以配置为准） |

**公开方法/槽：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| 构造 | `explicit MonitorPage(QWidget *parent = nullptr)` | `buildDefaultChannelConfig()` → `setupAlarmRules()` → `buildUi()` |
| 配置槽 | `void onChannelConfigReceived(const QVector<ChannelConfigInfo> &channels)` | 空配置忽略；通道数变化 → `rebuildChannelArea()`，否则 `applyConfigToCard(i)`；再 `setupAlarmRules()` |
| 数据入口 | `void onDataUpdated(const QVector<domain::DataPoint> &points)` | 以 `m_channelConfigs.size()` 为边界逐点驱动：chart.appendPoint、gauge.setValue、valueLabel（含单位+超限"!"）、LED 变色、AlarmEngine 评估 |
| 查询 | `bool isDemoRunning() const` | 演示源运行中 |
| 切换 | `void setDemoRunning(bool enabled)` | 启动/停止演示定时器 |

**私有关键逻辑：**

| 方法 | 职责 |
| --- | --- |
| `buildChannelCard(int index)` | 按 `m_channelConfigs[index]` 建卡片：信息列（nameLabel+LED+valueLabel）+ LineChartWidget + GaugeWidget |
| `rebuildChannelArea()` | `takeAt` 清空旧卡片（deleteLater 延迟析构防悬垂）→ 按配置数量重建 |
| `applyConfigToCard(int index)` | 通道数不变时只更新单卡片元数据（名称/单位/量程），不重建布局 |
| `buildDefaultChannelConfig()` | 默认 4 通道（温度℃0-100/压力MPa0-10/流量m3/h0-50/振动mm/s0-5），设备应答前兜底 |
| `setupAlarmRules()` | 先 `clearRules()` 再按 `rangeMax × kAlarmRatio` 为每通道添加 `AboveHigh` 规则（配置变化不叠加） |

### 2.14 `datascope::ui::RecordPage`

- **继承**：`public QWidget`
- **定位**：记录/回放页（按钮编排 + 进度条）。

**关键成员：** `datascope::services::RecordManager *m_manager`；记录组按钮（`m_startRecordBtn`/`m_stopRecordBtn`/`m_recordStateLabel`）；回放组控件（`m_loadReplayBtn`/`m_startReplayBtn`/`m_stopReplayBtn`/`m_replayFileLabel`/`m_replayProgress`）。

**方法签名与职责：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| 构造 | `explicit RecordPage(QWidget *parent = nullptr)` | `setObjectName("recordPage")`；`buildUi()` |
| 注入 | `void setRecordManager(datascope::services::RecordManager *manager)` | 连接 `progressChanged`/`replayFinished`/`errorOccurred` 信号 |
| 槽 | `onStartRecordClicked`/`onStopRecordClicked`/`onLoadReplayClicked`/`onStartReplayClicked`/`onStopReplayClicked`/`onReplayProgress(int)`/`onReplayFinished` | 按钮事件与进度更新 |

### 2.15 `datascope::simulator::SimulatorDevice`（V2-执行③）

- **继承**：`public QObject`
- **定位**：可复用模拟采集设备（TCP 服务端 + 周期数据帧 + 模式/异常注入）。独立程序 `simulator` 与集成测试共用。

**关键成员：**

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| `m_server` | `QTcpServer*` | TCP 服务端（`listen(QHostAddress::Any, port)`；port=0 系统分配随机端口） |
| `m_timer` | `QTimer*` | 数据帧定时器（默认 50ms = 20Hz） |
| `m_injector` | `FaultInjector` | 异常注入器（Error 模式生效） |
| `m_mode` | `SimulatorMode` | 运行模式（Normal/Alarm/Stress/Busy/Error） |
| `m_frameIntervalMs` / `m_channelCount` | `int` | 帧间隔 / 通道数（默认 4） |
| `m_phase` / `m_frameSeq` | `double` / `int` | 波形相位 / 帧序号（注入周期判据） |
| `m_clients` | `QVector<QTcpSocket*>` | 已连接客户端 |
| `m_clientParsers` | `QHash<QTcpSocket*, FrameParser>` | 每客户端独立解析器（多客户端不串帧） |
| `m_channelConfig` | `QVector<ChannelConfigInfo>` | 通道配置（查询响应数据源） |
| `m_alarmActive` / `m_alarmCounter` | `bool` / `int` | Alarm 模式：报警相位 / 每 20 帧翻转计数器 |

**公开接口：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| `start` | `bool start(quint16 port = 0)` | 监听 + 启动定时器；port=0 随机端口（测试用） |
| `port` / `clientCount` | `quint16` / `int` | 实际监听端口 / 连接客户端数 |
| `setMode` | `void setMode(SimulatorMode)` | 设模式；Stress 自动切 10ms + Sticky |
| `setFrameInterval` | `void setFrameInterval(int ms)` | 帧间隔（运行中修改立即生效） |
| `setChannelCount` | `void setChannelCount(int count)` | 通道数（<1 钳制为 1，重建配置） |
| `setFaultConfig` | `void setFaultConfig(const FaultConfig &)` | 注入异常配置（Error 模式生效） |
| `channelConfig` | `QVector<ChannelConfigInfo> channelConfig() const` | 当前通道配置（查询应答数据源） |

**信号：** `frameGenerated(int seq, const QByteArray &frame)`（每帧生成，观察/断言用）；`clientCountChanged(int count)`。

**核心流程（`onTick`）：** 相位递增 → Alarm 翻转检查 → Busy 忙窗口静默检查 → 构建正常帧 → 断线注入检查（触发则 `disconnectFromHost` 全部客户端并返回）→ `m_injector.process()` 得异常字节流 → 广播到所有 `ConnectedState` 客户端（每段一次 write）。

**客户端请求处理（`handleClientRequest`）：** 每客户端独立 parser 收集 → 收到 `0x03/0x01` 查询帧 → 以 `m_channelConfig` 编码应答 `0x83/0x01`；其它请求静默忽略。

### 2.16 `datascope::simulator::FaultInjector`（V2-执行③）

- **定位**：**纯逻辑**异常注入器（无 TCP/Qt 网络依赖）——正常帧字节 → 0..N 段异常字节流，每段对应一次 socket write。可单测、可复用（SimulatorDevice 与集成测试共用同一份注入逻辑）。

**关键方法：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| `setConfig` / `config` | `void` / `FaultConfig` | 设/取注入配置 |
| `process` | `QVector<QByteArray> process(int frameSeq, const QByteArray &normalFrame)` | 按 FaultType 变换：None 原样 / Sticky 两帧拼接 / Fragment 一帧拆两段 / CrcError 帧尾+1 / IllegalFrame LEN 改 0xFFFF / Delay 触发周期不发(空向量) / Surge 替换通道值并**重算 CRC** / Disconnect 返回空 |
| `shouldTrigger` | `bool shouldTrigger(int frameSeq)` | 触发判据：`period<=0 || frameSeq % period == 0` |
| `surgeChannel` | `QByteArray` | 突变：载荷 `[6 + ch*4, 6 + ch*4 + 4)` 替换为 surgeValue 大端，再对 FUNC..DATA 重算 MODBUS CRC16（低字节在前）——**帧保持合法**，使"突变 → 报警"链路可达 |

**设计要点：** 字节变换与"何时写 socket"解耦（分层）；Surge 重算 CRC 与 Delay/Disconnect 空向量是两类语义——前者的设备"活着"只是采集异常，后者是通信故障。

### 2.17 `datascope::ui::LogPanel`（V2-执行③）

- **继承**：`public QWidget`
- **定位**：实时日志面板（主窗口第 5 页签）。订阅全局 `LogManager::messageLogged`（观察者模式），与业务模块解耦——任何模块写日志都会实时流入面板。

**关键成员：**

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| `m_view` | `QPlainTextEdit*` | 只读日志视图（objectName "logView"） |
| `m_filterCombo` | `QComboBox*` | 过滤下拉（objectName "logFilterCombo"，All/Debug+/Info+/Warn+/Error+/Fatal，QVariant 存 LogLevel） |
| `m_buffer` | `QVector<LogEntry>` | 环形缓冲，上限 `kMaxEntries=1000`；超限 `rebuildView()` 整体重绘 |
| `m_filter` | `LogLevel` | 当前过滤级别（`level >= m_filter` 才显示） |

**行格式**：`[HH:mm:ss.zzz] [Level] [Module] message`；级别着色（Trace/Debug 灰、Info 浅绿、Warn 橙、Error 红、Fatal 亮红加粗）。

**公开接口：** 构造函数订阅 `LogManager::messageLogged` 并写入缓冲 → appendLine；`setLevelFilter`/`clear` 对应下拉与清空按钮。

**教学点：** 观察者模式（信号广播，类似 WPF 的事件聚合）；环形缓冲 + 整体重绘（性能上限有界）；黑盒 UI 测试用 `findChild` 定位控件。

### 2.18 `datascope::protocol::ChannelConfigCodec`（V2-执行③）

- **定位**：通道配置的协议编解码（契约 03 第 8 节）。`QVector<ChannelConfigInfo>` ↔ 载荷字节，单通道 33 字节定长无对齐。

**公开接口：**

| 方法 | 签名 | 职责 |
| --- | --- | --- |
| `encode` | `QByteArray encode(const QVector<ChannelConfigInfo> &channels)` | 通道数 <1 或 >255 → 空数组（失败）；否则 `[count:1B] + N×33B` |
| `decode` | `bool decode(const QByteArray &payload, QVector<ChannelConfigInfo> &out)` | 长度 ≠ `1+33×N` 或通道数为 0 → false 并清空 out；成功按偏移还原 name(16B UTF-8)/unit(8B)/min(4B BE)/max(4B BE) |

**防御要点：** 编解码都做长度/数量校验；UTF-8 名称超长截断、短名补零，解码正常还原。**协议层独立于网络层**，可纯单测（tst_channelconfigcodec）。

---

## 3. 类间关系（组合/依赖）

```
MainWindow（QMainWindow）
 ├── 持有 4 个页面（组合）：MonitorPage / DevicePage / RecordPage / SettingsPage
 ├── 持有 LogPanel* m_logPanel（组合，第 5 页签；订阅全局 LogManager）
 ├── 持有 DataService* m_service（组合，parent=this）
 ├── 持有 RecordManager* m_recordManager（组合，parent=this）
 │     └── RecordPage::setRecordManager 注入（依赖）
 └── 信号布线：
        DataService::dataUpdated ──→ MonitorPage::onDataUpdated
        DataService::dataUpdated ──→ RecordManager::appendData
        RecordManager::replayData ──→ MonitorPage::onDataUpdated
        DataService::channelConfigReceived ──→ MonitorPage::onChannelConfigReceived（V2-执行③）
        LogManager::messageLogged ──→ LogPanel（观察者订阅）

DeviceController（组合 DataService* m_service = new DataService(this)）
 └── 是 UI 的门面：状态机 + 重连（当前阶段 MainWindow 直接使用 DataService）

DataService（组合 QThread* + AcquisitionWorker* + QMutex*）
 ├── moveToThread 后 Worker 在采集线程运行
 ├── 通过信号槽（QueuedConnection）与 Worker 交互
 ├── 转发 Worker::pointsReady → dataUpdated；Worker::channelConfigReceived → channelConfigReceived
 └── 依赖 domain（DataPoint/Channel）、protocol（ChannelConfigInfo）、QThread/QMutex

AcquisitionWorker（组合 TcpClient* m_client，start 槽内创建）
 ├── 连接成功后发查询帧(0x03/0x01)；handleFrame 分流配置响应帧与采集数据帧
 └── TcpClient（组合 QTcpSocket* + FrameParser 值成员）
        └── 依赖 protocol 层（FrameBuilder/FrameParser/ProtocolFrame/ChannelConfigCodec）

RecordManager（组合 QTimer* m_replayTimer + QFile m_recordFile）
 └── 依赖 domain（DataPoint）

MonitorPage（组合 N× ChannelUi：LineChartWidget/GaugeWidget/LedIndicator/nameLabel/QLabel）
 ├── 依赖 domain（DataPoint）、protocol（ChannelConfigInfo——配置数据驱动渲染）
 └── 组合 AlarmEngine + AlarmEventModel（V2 报警中心，与 View 分离可单测）

LogPanel（组合 QPlainTextEdit + QComboBox + 环形缓冲 QVector<LogEntry>）
 └── 依赖 infrastructure（LogManager::messageLogged）

SimulatorDevice（独立程序 + 集成测试共用，依赖 protocol 层）
 ├── 组合 QTcpServer + QVector<QTcpSocket*> + QHash<QTcpSocket*, FrameParser>
 ├── 组合 FaultInjector（值成员）→ 纯逻辑异常变换
 ├── 组合 QVector<ChannelConfigInfo> m_channelConfig（查询应答数据源）
 └── 使用 FrameBuilder 组帧 / FrameParser 解析 / ChannelConfigCodec 编解码
```

依赖关系文本图：

```
UI 层 ──► services 层 ──► protocol 层 ──► utils 层（ByteUtils 被协议层使用）
        │                  └─► domain 层
        └──► domain 层
simulator 层 ──► protocol 层 / QtNetwork（不依赖 UI 与 services）
infrastructure 层（被 main/全局使用）  ◄── 任意层可依赖
domain 层 / protocol 层 只依赖 QtCore
```

---

## 4. 关键状态机：DeviceController

### 4.1 状态定义（`datascope::domain::DeviceStatus`）

| 状态 | 枚举值 | 含义 |
| --- | --- | --- |
| `Disconnected` | 0 | 未连接（初始状态） |
| `Connecting` | 1 | 正在连接（异步过程中） |
| `Connected` | 2 | 已连接（采集可用） |
| `Error` | 3 | 出错（通信异常/设备离线） |

### 4.2 状态转移图

```
                     connectDevice()
  ┌────────────┐  ──────────────────►  ┌─────────────┐
  │ Disconnected│                        │ Connecting  │
  └────────────┘  ◄──────────────────  └─────────────┘
      ▲     ▲          主动断开              │     │
      │     │  disconnectDevice()           │     │  connectDevice() 失败
      │     │                               │     ▼
      │     │                         ┌─────────────┐
      │     └─────────────────────────│   Error     │
      │        用户主动断开            └─────────────┘
      │                                    │  意外断线 / 错误
      │  onServiceConnected()              ▼
      │                          ┌──────────────────┐
      └──────────────────────────│    Connected     │
                                 └──────────────────┘
                                     │  意外断线（设备掉电/网线断开）
                                     ▼
                                ┌─────────┐
                                │  Error  │ ──► startReconnectTimer() 退避重连
                                └─────────┘
```

### 4.3 转移表

| 触发事件 | 前提条件 | 动作 | 下一状态 |
| --- | --- | --- | --- |
| `connectDevice()` | 当前非 Connected | 保存 host/port；`m_userDisconnected=false`；`m_retryAttempts=0`；停重连定时器；`m_service->connectTo` | Connecting |
| `onServiceConnected()` | — | `m_userDisconnected=false`；`m_retryAttempts=0`；停定时器 | Connected |
| `onServiceDisconnected()` | `m_userDisconnected==true` | 复位标志 | Disconnected |
| `onServiceDisconnected()` | 意外断线 | 启动退避重连 | Error |
| `onServiceError(msg)` | — | emit errorOccurred；非主动断开则启动重连 | Error |
| `onReconnectTimeout()` | — | 重新 `m_service->connectTo(m_host, m_port)` | Connecting |
| `disconnectDevice()` | — | `m_userDisconnected=true`；停定时器；`m_service->disconnectFromDevice()` | Disconnected |

### 4.4 退避重连策略

- 间隔计算：`intervalMs = min(1000 << m_retryAttempts, 30000)`（1s→2s→4s→8s→16s→30s，封顶 30s）。
- 每次超时重连前 `++m_retryAttempts`，连接成功后归零。
- 仅当 `m_status == Error` 且 `m_userDisconnected == false` 且定时器未激活时才启动，防止"断线+错误"双重触发让退避跳档。
- 重连每次都是**新建全新连接**：Worker 在错误时 `deleteLater()` 并置空 `m_client`，保证 `start()` 不因残留客户端吞掉重连请求。

---

## 5. 数据格式设计

### 5.1 CSV 记录格式（RecordManager 落盘）

**表头：**
```
timestampMs,channelIndex,value
```

**数据行：** 每行一个采样点，`QTextStream` 按行写，UTF-8 文本：

```
timestampMs,channelIndex,value
1734168000123,0,42.5
1734168000123,1,5.2
1734168000123,2,25.8
1734168000123,3,3.1
```

| 列 | 类型 | 说明 |
| --- | --- | --- |
| `timestampMs` | qint64 | `QDateTime::toMSecsSinceEpoch()`，自 1970-01-01 的毫秒数 |
| `channelIndex` | int | 通道序号（0..3） |
| `value` | double | 采样值（工程值，已换算） |

**说明：**
- 同一时间戳下的多行 = 同一采集帧的多个通道（模拟设备一帧 4 通道 → 4 行）。
- 文件以 `WriteOnly|Text|Truncate` 打开，写入即覆盖。
- `startRecording` 时写表头；`appendData` 仅当记录开关开启且文件打开时写行。

### 5.2 回放格式与重组

**加载（`loadReplay`）：**
1. 以 `ReadOnly|Text` 打开 CSV；
2. 跳过表头行（`reader.readLine()` 一次）；
3. 逐行 `split(',')`，不足 3 列视为畸形行跳过（不中断整个文件）；
4. 按时间戳分组：`QHash<qint64, QVector<DataPoint>> framesByTime`；
5. 取全部时间戳键 → `std::sort` 升序 → 依序构建 `m_replayFrames`（QHash 无序，必须显式排序保证回放顺序）。

**回放（`startReplay(intervalMs=100)`）：**
- `QTimer` 每 tick 发一帧 `replayData(m_replayFrames.at(m_replayIndex))`；
- 默认 100ms/帧（10 帧/秒）；
- 发完最后一帧：停定时器、`emit replayFinished()`；
- 进度信号 `progressChanged(current)` 驱动 `RecordPage` 的 `QProgressBar`。

### 5.3 通信帧字节序约定（协议层）

| 字段 | 字节序/顺序 |
| --- | --- |
| SOF | `0xAA 0x55`（固定） |
| LEN | 大端序（高字节在前） |
| DATA 内 float | 大端序 4 字节（模拟设备采集帧；PC 内存小端，需手动重排） |
| CRC16 | MODBUS CRC16；帧尾"低字节在前"追加（先 CRC_L 再 CRC_H） |
| CRC 覆盖范围 | FUNC(1) + CMD(1) + LEN(2) + DATA(len)，即 frame 跳过 SOF 的所有字节 |

---

> 本文档与工程源码保持一致；后续若代码演进，请同步更新对应章节。
